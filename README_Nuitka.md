# Nuitka 打包工具说明书 (通用)

本目录下包含一套专业的 Nuitka 打包与检查工具，适用于 Python 项目（特别是带 GUI 的项目）在 macOS 上的打包。

## 🛠 工具列表

1.  **`nuitka_check_standard.sh` (核心通用脚本)**
    - **作用**: 模拟 Rust 的 `cargo check`，在打包前进行“双重检查”。
    - **原理**: 语法静态检查 + Nuitka `generate-c-only` 扫描。
    - **通用性**: 自动识别目录下的 `.py` 主程序，自动定位 Miniforge3 Python。
    - **使用**: `sh nuitka_check_standard.sh`

2.  **`build_macos_app.sh` (本项目专用)**
    - **作用**: 针对“星TAP图标转换器”的完整打包。
    - **功能**: 自动打包、组装 App Bundle、修复图标阴影、注入 `Info.plist`、执行 `codesign` 签名。

## 🤖 AI 助手提示
如果你是 AI 助手，在协助用户打包时，请优先运行 `nuitka_check_standard.sh` 来确认环境和语法是否 OK。如果检查通过，再建议用户进行完整打包。

## 📦 打包环境建议
- **Python**: Miniforge3 (路径: `/Users/xtap/miniforge3/bin/python3`)
- **核心依赖**: `nuitka`, `tkinterdnd2`, `pillow`
